define(['/app/controllers/module.js', 'pagination'], function (controllers) {
	'use strict';
    controllers.controller("About", function($http, $rootScope, $scope, $translate, $stateParams, Constants) {
    	console.log("=====About" + Constants.SUCCESS);

	})
});
